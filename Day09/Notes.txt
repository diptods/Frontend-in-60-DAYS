Topic : Tailwind Layout & Components 
Project : Modern Gym Website 


1. Tailwind CSS Container:
    The container class sets responsive fixed widths at different breakpoints, ensuring the content adapts to various screen sizes. 
    To center the container, we use mx-auto utility class. It adjust the margin of the container automatically so that the container appears to be in center. 
2. Tailwind CSS Box Sizing: Box sizing in CSS controls how a browser calculates a web element's total width and height. Tailwind CSS gives you two straightforward utility classes to manage this behavior instantly.
    it mainly known as  box-sizing in CSS , But Tailwind CSS is utility-first so it uses two class (i) box-content  & (ii) box-border 

        box-border(Default) :   width(Total Width Formula) ---- Padding and border shrink the inner content area.
        box-content         :   width + padding + border   ---- Padding and border push the element outward, making it bigger.

3. Tailwind CSS Display :
    Display classes in Tailwind CSS control how elements behave in the layout flow and how they occupy space on the screen. They directly map to the standard CSS display property.
        block : Takes up the full width of its parent container.Starts on a new line.Respects top and bottom margins and paddings.
        inline-block : Takes up only the width of its content.Sits next to other elements on the same line.Respects top and bottom margins, paddings, width, and height.
        inline : Sits inline with text and content.Ignores width, height, and top/bottom margins.
        hidden : Completely removes the element from the page layout.It takes up zero space, making it perfect for toggling menus.
        flex : Turns the element into a Flexbox container.Aligns its direct child elements in rows or columns instantly.
        grid : Turns the element into a CSS Grid container.Perfect for building precise rows and columns.

4. Tailwind CSS Float: 
    Float classes in Tailwind CSS control how elements shift to the left or right, allowing text to wrap naturally around them. While modern web layouts mostly use Flexbox and Grid, floats are still the best tool for simple text-wrapping tasks.
        float-left : Pushes the element to the far left side of its parent box.Text and inline items wrap around its right side.
        float-right : Pushes the element to the far right side of its parent box.Text and inline items wrap around its left side.
        float-none : Resets the element to its default placement in the layout flow.Text will not wrap around it.


5. Tailwind CSS Clear:
        clear-both: Ensures an element drops down below both left and right floated items.
        clear-left: Drops down below left floated items.
        clear-right: Drops down below right floated items.
        clearfix :  Automatically stretches the container to fit the floated items, preventing layout breakage.

6. Tailwind CSS Object Fit:
    Object fit classes in Tailwind CSS control how an image or video stretches, crops, or resizes to fit inside its container. They act like a smart cropping tool for media elements.
    Object fit Classes Are (i)object-contain ,(ii)object-cover ,(iii)object-fill ,(iv)object-none & (v)object-scale-down
        object-contain: Scales the image so the entire picture is visible inside the container.Behavior: It prevents cropping, which often leaves empty black or white bars on the sides (letterboxing).
        object-cover: Scales the image to completely fill the container.Behavior: It crops the sides or top/bottom so the image stays perfectly proportioned without squishing.
        object-fill: Stretches or squishes the image to match the container's exact dimensions.Behavior: This will distort your image if the aspect ratios don't match.
        object-none: Ignores the container size completely and displays the image at its original, raw size.Behavior: It crops out anything that doesn't fit.
        object-scale-down: Compares the image size to the container size.Behavior: It acts like object-none or object-contain, whichever results in a smaller image.

7. Tailwind CSS Object Position:
    Object position classes in Tailwind CSS control the alignment of an image or video inside its container. They work hand-in-hand with object-cover or object-contain to let you choose exactly which part of a cropped photo remains visible.
        object-center : Centers the image perfectly both horizontally and vertically.
        object-top    : Aligns the image to the top edge of the container. Perfect for portraits so heads don't get cropped out.
        object-bottom : Aligns the image to the bottom edge of the container.
        object-left   : Aligns the image to the left edge.
        object-right  : Aligns the image to the right edge.

        Corner Alignments:
            object-top-left / object-top-right object-bottom-left / object-bottom-right

            The Banner Fix (object-top) | Custom Arbitrary Positions (object-[20%_80%]) | Responsive Camera Focus (w-full h-48 object-cover object-center lg:object-right)
                    object-left-top   | object-left   | object-left-bottom
                    object-top        | object-center | object-bottom
                    object-right-top  | object-right  | object-right-bottom

8. Tailwind CSS Overflow: 
    Overflow classes in Tailwind CSS control what happens when an element's content is too large to fit inside its specified width or height. It determines whether the extra content is hidden, creates a scrollbar, or spills outside the box.
        overflow-auto       : It automatically adds a scrollbar whenever it is required. This class adds scrollbars to an element in the event that its content overflows the boundary of that element.
        overflow-hidden     : The overflow is clipped and the rest of the content is invisible. This class is used to clip any content within an element that overflows the bounds of that element.
        overflow-visible    : The content is not clipped and visible outside the element box. This class used to prevent content within an element from being clipped.
        overflow-scroll     : The overflow is clipped but a scrollbar is added to see the rest of the content. The scrollbar can be horizontal or vertical. This class is used when you need to show scrollbars, this utility will only be shown if scrolling is necessary.
        overflow-x-auto     : This class specifies whether to add a scroll bar, clip the content, or display overflow content of a block-level element when it overflows at the left and right edges. The overflow-x-auto provides a scrolling mechanism for overflowing boxes.
        overflow-y-auto     : This class specifies whether to add a scroll bar, clip the content, or display overflow content of a block-level element when it overflows at the top and bottom edges. The overflow-y-auto class provides a scrolling mechanism for overflowing boxes.
        overflow-x-hidden   : It is used to clip the content and no scrolling mechanism is provided on the x-axis.
        overflow-y-hidden   : It is used to clip the content and no scrolling mechanism is provided on the y-axis.
        overflow-x-visible  : This class does not clip the content. The content may be rendered outside the left and right edges.
        overflow-y-visible  : This class does not clip the content. The content may be rendered outside the left and right edges.
        overflow-x-scroll   : It is used to clip the content and providing a scrolling mechanism.
        overflow-y-scroll   : It is used to clip the content and also provides a scrolling mechanism.

9. Tailwind CSS OverScroll :
    This class accepts more than one value in tailwind CSS. It is the alternative to the CSS Overscroll-behavior property. This class is used to set the behavior of the browser when the boundary of a scrolling area is reached. This property can be used to prevent unwanted scrolling in pages where there are multiple scroll areas. 
        overscroll-auto         : It is used to set the scrolling behavior to default. The whole page along with the element will scroll even if the boundary of the element is reached. It is the default value.
        overscroll-contain      : It is used to set the scrolling behavior to default only on the element used. Scrolling the element further after it has reached the boundary will not scroll the elements behind it. No scroll-chaining would occur in the neighboring scrolling areas.
        overscroll-none         : It is used to prevent scroll-chaining on all elements. The default scroll overflow behavior is also prevented.
        overscroll-y-auto       : This is used to set the scrolling behavior on the y-axis to default on all the elements. The whole page will scroll even if the boundary of the element is reached. It is the default value.
        overscroll-y-contain    : It is used to set the scrolling behavior on the y-axis to default only on the element used. No scroll-chaining would occur on the neighboring scrolling areas and the elements behind will not scroll.
        overscroll-y-none       : It is used to prevent scroll-chaining on the y-axis on all elements. The default scroll overflow behavior is also prevented.
        overscroll-x-auto       : It is used to set the scrolling behavior on the x-axis to default on all the elements. The whole page will scroll even if the boundary of the element is reached. It is the default value.
        overscroll-x-contain    : It is used to set the scrolling behavior on the x-axis to default only on the element used. No scroll-chaining would occur on the neighboring scrolling areas and the elements behind will not scroll.
        overscroll-x-none       : It is used to prevent scroll-chaining on the x-axis on all elements. The default scroll overflow behavior is also prevented.

10.  Tailwind CSS Position : 
    This class accepts more than one value in tailwind CSS. It is the alternative to the CSS Position property. This class is used for controlling how an element is positioned in the DOM.
        static      : This class is used to set the position of an element according to the normal flow of the document.
        fixed       : This class will be positioned fixed to the viewport. An element with fixed positioning allows it to remain at the same position even we scroll the page. We can set the position of the element using the top, right, bottom, left.
        absolute    : This class is used to set the position of an element outside the normal flow of the document, causing neighboring elements to act as if the element doesn't exist.
        relative    : This class is used to set the position of an element relative to the normal flow of the document.
        sticky      : This class is used to set the position of an element as relative until it crosses a specified threshold, then it treats it as fixed until its parent is off-screen.

11. Tailwind CSS Top/Left/Bottom/Right :
    These classes accept many values in tailwind CSS in which all the properties are covered in class form. These are the alternative to the CSS Top/Right/Bottom/Left properties. These classes are used to control the alignment of a positioned element. Remember we can use these properties only with positioned elements.
        .inset-0    : It is used to provide 0px value to top/right/bottom/left properties of element.
        .inset-y-0  : It is used to provide value 0px to the top and bottom property of the element.
        .inset-x-0  : It is used to provide value 0px to the right and left property of the element.
        .top-0      : It is used to provide value 0px to the top property of the element.
        .right-0    : It is used to provide value 0px to the right property of the element.
        .bottom-0   : It is used to provide value 0px to the bottom property of the element.
        .left-0     : It is used to provide value 0px to the left property of the element. 

12. Tailwind CSS Visibility :
    This class accepts two values in tailwind CSS. It is the alternative to the CSS visibility property. This class is used to specify whether an element is visible or not in a web document but the hidden elements take up space in the web document. Use the display property to remove or hide and delete an element from the browser.
        invisible: This class is used to hide the element from the page but takes up space in the document.
        visible: It is the default value. The element is shown or visible normally in the web document.

13. Tailwind CSS Z-index  :
    The tailwind CSS is a utility CSS framework that provides classes the manage our HTML content in the use of CSS. The tailwind CSS makes our designing part easier and responsive for multiple platforms. The z-Index utility is for controlling the stack order of an element. It is the alternative to the CSS z-index property.
        z-0     : It is used to give the z-index 0 to the element. 
        z-10    : It is used to give the z-index 10 to the element. The elements appear 10px upside along the z-axis.
        z-20    : It is used to give the z-index 20 to the element. The elements appear 20px upside along the z-axis.
        z-30    : It is used to give the z-index 30 to the element. The elements appear 30px upside along the z-axis.
        z-40    : It is used to give the z-index 40 to the element. The elements appear 40px upside along the z-axis.
        z-50    : It is used to give the z-index 50 to the element. The elements appear 50px upside along the z-axis.
        z-auto  : It is used to give the z-index auto to the element. The elements appear auto upside along the z-axis.

## Tailwind CSS Flexbox ##  Tailwind CSS allows you to create flexible and responsive layouts using Flexbox.

14. Tailwind CSS Flex Direction : 
    The CSS flexbox is a vital feature to develop the frontend, there are four directions available in CSS so in tailwind CSS all the properties are covered as in class form. It is the alternative of CSS flex-direction Property for fast development of front-end.
        flex-row            : It arranges the row the same as the text direction. The default value of flex-direction is a row. It is used to specify that the item has a normal text direction. It makes the item follow the normal text direction in lines.
        flex-row-reverse    : This class is used to follow the opposite text direction. It makes flex items in reverse order exactly the opposite of text direction as we can see in the Output.
        flex-col            : It arranges the row as a column same as text direction but top to bottom. It is used to specify that the item has a normal top to bottom direction. It makes the item follow the normal top to bottom direction as we can see in the output.
        flex-col-reverse    : It arranges the row as a column same as row-reverse bottom to top. It is used to specify that the item has a normal bottom to top direction. It makes the item follow the normal bottom to top direction as we can see in the output.

15. Tailwind CSS Flex Wrap :
    The CSS flexbox is a vital feature to develop the frontend, there are three wraps available in CSS so in tailwind CSS all the properties are covered as in class form. It is the alternative of CSS flex-wrap Property for fast development of front-end.
        flex-wrap           : This class is used to break the flex item into multiples lines. It makes flex items wrap to multiple lines according to flex item width.
        flex-nowrap         : The default value of wrap-flex is nowrap. It is used to specify that the item has no wrap. It makes item wrap in single lines.
        flex-wrap-reverse   : This class is used to reverse the flow of the flex items when they wrap to new lines.
16. Tailwind CSS Flex :
    The CSS flexbox is a vital feature to develop the frontend, there is four flex available in tailwind CSS all the properties are covered as in class form. It is the alternative of CSS flex Property for fast development of front-end. It is used to set the length of flexible items. The flex class is much responsive and mobile-friendly. It is easy to positioning child elements and the main container. The margin doesn’t collapse with the content margins. The order of any element can be easily changed without editing the HTML section.
        flex-1          : A ratio that specifies, how much items will grow relative to the rest of the flexible items. It has been used to allow a flex item to grow and shrink as needed, ignoring its initial size.
        flex-initial    : This class defines how much that item will grow relative to the rest of the flexible items. It has been used to allow a flex item to shrink but not grow, taking into account its initial size
        flex-auto       : This class specifies, how much that item will grow relative to the content of the flexible items. It has been used to allow a flex item to grow and shrink as needed, ignoring its initial size
        flex-none       : This class set the restriction to growing or shrinking the element if extra space available or not. This is to prevent a flex item from growing or shrinking, it only takes the space according to the size of the content

17. Tailwind CSS Flex Grow: 
        flex-grow-0     : This class restricts the growing feature, it has the ability of an item to grow compared to content present inside the same container.
        flex-grow       : This class does not restrict the growing feature, it has the ability of an item to grow compared to the space left on the screen.

18. Tailwind CSS Flex Shrink :
        flex-shrink-0: This class is used to prevent the shrinking of an element.
        flex-shrink: This class is used to shrink an element.
        
19. Tailwind CSS Order :
        order-1  |  order-2   |  order-3  |  order-4  |  order-5  |  order-6  |
        order-7  |  order-8   |  order-9  |  order-10 |  order-11 |  order-12 |
        order-first  |  order-last  |  order-none |

 ####       Tailwind CSS Grid       #### 

20. Tailwind CSS Grid Template Columns :
        grid-cols-1: Each row concedes only one column.
        grid-cols-2:  Each row concedes only two columns.
        grid-cols-3:  Each row concedes only three columns.
        grid-cols-4:  Each row concedes only four columns.
        grid-cols-5:  Each row concedes only five columns.
        grid-cols-6:  Each row concedes six columns.
        grid-cols-7: Each row concedes seven-columns.
        grid-cols-8: Each row concedes eight columns.
        grid-cols-9: Each row concedes nine columns.
        grid-cols-10: Each row concedes ten columns.
        grid-cols-11: Each row concedes eleven columns.
        grid-cols-12: Each row concedes twelve columns.
        grid-cols-none: Does not follow the grid-column property.

21. Tailwind CSS Grid Column Start / End :
        col-auto    |   col-span-1  |   col-span-2  |   col-span-3  |   col-span-4  |   col-span-5  |   col-span-6  |   col-span-7  |   col-span-8  |   col-span-9      |   col-span-10     |   col-span-11     |   col-span-12   |   col-span-full 
        col-start-auto  |   col-start-1     |   col-start-2     |   col-start-3     |   col-start-4     |   col-start-5     |   col-start-6     |   col-start-7     |   col-start-8     |   col-start-9     |   col-start-10    |   col-start-11    |   col-start-12    |   col-start-13
        col-end-auto    |   col-end-1   |   col-end-2   |   col-end-3   |   col-end-4   |   col-end-5   |   col-end-6   |   col-end-7   |   col-end-8   |   col-end-9   |   col-end-10  |   col-end-11  |   col-end-12  |   col-end-13

22. Tailwind CSS Grid Template Rows :
        grid-rows-1: Each row concedes only one row.
        grid-rows-2: Each row concedes only two rows.
        grid-rows-3: Each row concedes only three rows.
        grid-rows-4: Each row concedes only four rows.
        grid-rows-5: Each row concedes only five rows.
        grid-rows-6: Each row concedes six rows.
        grid-rows-none: Does not follow the grid-row property.
        
23. Tailwind CSS Grid Row Start / End :
        row-auto , row-span-1 , row-span-2 , row-span-3 , row-span-4 , row-span-5, row-span-6, row-span-7, row-span-8, row-span-9, row-span-10, row-span-11, row-span-12, row-span-full
        row-start-1, row-start-2, row-start-3, row-start-4 , row-start-5, row-start-6, row-start-7, row-start-8, row-start-9, row-start-10, row-start-11, row-start-12 , row-start-13, row-start-auto
        row-end-1, row-end-2, row-end-3, row-end-4, row-end-5, row-end-6, row-end-7, row-end-8, row-end-9, row-end-10, row-end-11, row-end-12, row-end-13, row-end-auto

24. Tailwind CSS Grid Auto Flow :
        grid-flow-row       :   Auto-placement algorithm place the items by filling each row in turn, adding new rows as necessary.
        grid-flow-col       :   Auto-placement algorithm place the items by filling each column in turn, adding new columns as necessary.
        grid-flow-row-dense :   Specifying that the auto-placement algorithm uses a "dense" packing algorithm for the column.
        grid-flow-col-dense :   Specifying that the auto-placement algorithm uses a "dense" packing algorithm for rows.

25. Tailwind CSS Grid Auto Columns :
        auto-cols-auto  :   It is the default value. The size is determined implicitly according to the size of the container.
        auto-cols-min   :   It specifies the size depending on the smallest item in the container.
        auto-cols-max   :   It specifies the size depending on the largest item in the container.
        auto-cols-fr    :   It specifies the size in the range of [min, max] greater than or equal to min and less than or equal to max.

26. Tailwind CSS Grid Auto Rows :
        auto-rows-auto  :   It is the default value. The size is determined implicitly according to the size of the container.
        auto-rows-min   :   It specifies the size depending on the smallest item in the container.
        auto-rows-max   :   It specifies the size depending on the largest item in the container.
        auto-rows-fr    :   It specifies the size in the range of [min, max] greater than or equal to min and less than or equal to max.

27. Tailwind CSS Gap :
        | gap-0 / gap-x-0 / gap-y-0 | 0rem | 0px |
        | gap-px / gap-x-px / gap-y-px | 1px | 1px |
        | gap-0.5 / gap-x-0.5 / gap-y-0.5 | 0.125rem | 2px |
        | gap-1 / gap-x-1 / gap-y-1 | 0.25rem | 4px |
        | gap-1.5 / gap-x-1.5 / gap-y-1.5 | 0.375rem | 6px |
        | gap-2 / gap-x-2 / gap-y-2 | 0.5rem | 8px |
        | gap-2.5 / gap-x-2.5 / gap-y-2.5 | 0.625rem | 10px |
        | gap-3 / gap-x-3 / gap-y-3 | 0.75rem | 12px |
        | gap-3.5 / gap-x-3.5 / gap-y-3.5 | 0.875rem | 14px |
        | gap-4 / gap-x-4 / gap-y-4 | 1rem | 16px |
        | gap-5 / gap-x-5 / gap-y-5 | 1.25rem | 20px |
        | gap-6 / gap-x-6 / gap-y-6 | 1.5rem | 24px |
        | gap-7 / gap-x-7 / gap-y-7 | 1.75rem | 28px |
        | gap-8 / gap-x-8 / gap-y-8 | 2rem | 32px |
        | gap-9 / gap-x-9 / gap-y-9 | 2.25rem | 36px |
        | gap-10 / gap-x-10 / gap-y-10 | 2.5rem | 40px |
        | gap-11 / gap-x-11 / gap-y-11 | 2.75rem | 44px |
        | gap-12 / gap-x-12 / gap-y-12 | 3rem | 48px |
        | gap-14 / gap-x-14 / gap-y-14 | 3.5rem | 56px |
        | gap-16 / gap-x-16 / gap-y-16 | 4rem | 64px |
        | gap-20 / gap-x-20 / gap-y-20 | 5rem | 80px |
        | gap-24 / gap-x-24 / gap-y-24 | 6rem | 96px |
        | gap-28 / gap-x-28 / gap-y-28 | 7rem | 112px |
        | gap-32 / gap-x-32 / gap-y-32 | 8rem | 128px |
        | gap-36 / gap-x-36 / gap-y-36 | 9rem | 144px |
        | gap-40 / gap-x-40 / gap-y-40 | 10rem | 160px |
        | gap-44 / gap-x-44 / gap-y-44 | 11rem | 176px |
        | gap-48 / gap-x-48 / gap-y-48 | 12rem | 192px |
        | gap-52 / gap-x-52 / gap-y-52 | 13rem | 208px |
        | gap-56 / gap-x-56 / gap-y-56 | 14rem | 224px |
        | gap-60 / gap-x-60 / gap-y-60 | 15rem | 240px |
        | gap-64 / gap-x-64 / gap-y-64 | 16rem | 256px |
        | gap-72 / gap-x-72 / gap-y-72 | 18rem | 288px |
        | gap-80 / gap-x-80 / gap-y-80 | 20rem | 320px |
        | gap-96 / gap-x-96 / gap-y-96 | 24rem | 384px |

####  Tailwind CSS Alignment #####

28. Tailwind CSS Justify Content: 
        justify-start
        justify-end 
        justify-center 
        justify-between 
        justify-around 
        justify-evenly 

29. Tailwind CSS Justify Items:
        justify-items-auto 
        justify-items-start 
        justify-items-end 
        justify-items-center 
        justify-items-stretch 

30. Tailwind CSS Justify Self   :
        justify-self-auto 
        justify-self-start 
        justify-self-end 
        justify-self-center 
        justify-self-stretch 

31. Tailwind CSS Align Content :
        content-center 
        content-start 
        content-end 
        content-between 
        content-around 
        content-evenly 

32. Tailwind CSS Align Items:
        items-start 
        items-end 
        items-center 
        items-baseline 
        items-stretch

33. Tailwind CSS Align Self:
        self-auto 
        self-start 
        self-end 
        self-center 
        self-stretch 

34. Tailwind CSS Background Size:
        bg-auto
        bg-cover
        bg-contain
35........{many more from [https://www.geeksforgeeks.org/css/tailwind-css/]}


